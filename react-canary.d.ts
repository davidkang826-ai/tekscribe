// App Router runs on Next's vendored React canary, which ships
// <ViewTransition>. Stable @types/react keeps its types behind this
// reference (see @types/react/canary.d.ts).
/// <reference types="react/canary" />
